We found a specific UPI payment issue in the Deposit / Add Cash flow.

DO NOT change the UPI ID.
DO NOT change the minimum deposit amount.
DO NOT change the wallet logic.
DO NOT change the payment proof flow.
DO NOT change the UI/design.
DO NOT add unrelated features.

Problem:
- UPI ID: jazment@ybl
- Direct/manual payment to this same UPI ID works normally.
- Example: ₹500 payment works when the user manually pays the UPI ID.
- When the user taps our "Pay Now" button, the UPI app opens with the same UPI ID and amount prefilled, but the payment fails with a bank-limit error.
- This problem started after the UPI deeplink was introduced and has existed for approximately 2 months.

This strongly suggests the generated UPI deeplink/request parameters are causing the payment app/bank to treat the transaction differently.

WORK ONLY ON THE UPI DEEPLINK IMPLEMENTATION.

First inspect the exact URI generated by the current "Pay Now" button.

Check every parameter being sent, including:
- pa
- pn
- am
- cu
- tn
- tr
- mc
- any other parameters
- URI encoding
- amount formatting
- transaction/reference generation
- merchant/category information if present

Compare the generated deeplink with the simplest valid UPI payment URI for paying the configured UPI ID and amount.

IMPORTANT:
Do not blindly add parameters.
Do not invent a merchant code.
Do not use a random transaction/reference value.
Do not hardcode a fake merchant identity.

Determine which parameter or formatting difference is causing the payment app to reject the deeplink transaction while direct UPI-ID payment works.

The desired behavior is:

User selects ₹500
→ taps Pay Now
→ UPI app chooser opens
→ selected UPI app opens
→ jazment@ybl is the payee
→ ₹500 is prefilled
→ user authorizes payment
→ payment should proceed normally

Also test amounts:
₹200
₹500
₹1000
₹2000

Test the deeplink on Android with the available UPI apps.

Before changing code, report:
1. Current deeplink format.
2. All parameters currently being sent.
3. Which parameter(s) are suspicious/incorrect.
4. The corrected deeplink format you intend to use.

Then make ONLY the necessary deeplink correction.

After the change, verify TypeScript/build validation.

Do NOT publish OTA.
Do NOT build an APK.
Do NOT modify anything unrelated.